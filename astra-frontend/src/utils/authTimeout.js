// import Cookies from "js-cookie";

// export const setAutoLogout = (handleLogout, timeoutMinutes = 500) => {
//   const timeout = setTimeout(() => {
//     if (Cookies.get("authData")) {
//       handleLogout();
//     }
//   }, timeoutMinutes * 60 * 1000); // Convert minutes to milliseconds

//   return timeout;
// };

// export const clearAutoLogout = (timeout) => {
//   clearTimeout(timeout);
// };
